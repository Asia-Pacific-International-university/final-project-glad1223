import 'package:flutter/material.dart';

class ActiveQuestScreen extends StatelessWidget {
  const ActiveQuestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement active quest screen UI
    return Scaffold(
      appBar: AppBar(title: const Text('Active Quest')),
      body: const Center(child: Text('Active Quest Screen')),
    );
  }
}
