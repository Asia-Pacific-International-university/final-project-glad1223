import 'package:flutter/material.dart';

class QuestResultScreen extends StatelessWidget {
  const QuestResultScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement quest result screen UI
    return Scaffold(
      appBar: AppBar(title: const Text('Quest Result')),
      body: const Center(child: Text('Quest Result Screen')),
    );
  }
}
